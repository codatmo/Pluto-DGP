build:
  docker:
    web: Dockerfile
run:
  web: julia --project=/home/pluto/pluto-deployment-environment  -e 'import PlutoSliderServer; PlutoSliderServer.run_directory(\".\"; SliderServer_port=1234 , SliderServer_host=\"0.0.0.0\")'
